import { ButtonProps } from './Button.types';
declare const Button: ({ className, color, size, variant, children, ...rest }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export default Button;
