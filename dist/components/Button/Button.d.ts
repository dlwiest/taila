import { ButtonProps } from './Button.types';
declare const Button: ({ className, color, rounded, size, variant, loading, children, ...rest }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export default Button;
